{
  "name": "server-survival-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build-pages.js",
    "generate-env": "node scripts/generate-env-config.js"
  }
}
