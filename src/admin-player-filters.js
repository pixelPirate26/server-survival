window.ADMIN_PLAYER_FILTERS = Object.freeze({
    campus: Object.freeze({
        allLabel: "All Campuses",
        options: Object.freeze(["RR", "EC"]),
    }),
    branch: Object.freeze({
        allLabel: "All Branches",
        options: Object.freeze(["CSE", "AIML"]),
    }),
    section: Object.freeze({
        allLabel: "All Sections",
        options: Object.freeze("ABCDEFGHIJKL".split("")),
    }),
    username: Object.freeze({
        placeholder: "Simple string match",
    }),
});
